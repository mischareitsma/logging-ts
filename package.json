{
  "name": "logging-ts",
  "version": "1.0.0",
  "description": "TypeScript Logging Capabilities",
  "main": "out/index.js",
  "types": "out/index.d.ts",
  "scripts": {
    "clean": "rm -rf out/ dist/",
    "compile": "tsc -p tsconfig.json",
    "dist": "tsc -p tsconfig.build.json && cp package.json dist/",
    "lint": "npx eslint .",
    "build": "npm run clean && npm run lint && npm run compile && npm run test && npm run dist",
    "test": "jest"
  },
  "author": "Mischa Reitsma",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.6",
    "@types/node": "^20.8.7",
    "@typescript-eslint/eslint-plugin": "^6.8.0",
    "@typescript-eslint/parser": "^6.8.0",
    "eslint": "^8.52.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
